- defaults:
    link:
      create: true
      relink: true
      force: true

- link:
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.agignore: agignore
    ~/.gitignore: gitignore
    ~/.config:
      glob: true
      path: config/*
      create: true
    ~/.vim:
      glob: true
      path: vim/*
    ~/.gitconfig: config/.gitconfig

- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]
